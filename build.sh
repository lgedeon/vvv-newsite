# creates a {$project}.json or {$project}.sh file that identifies the
# sites we want to set up, sql-dumps of content for those sites (optional),
# media.zip content to populate the media library (optional)